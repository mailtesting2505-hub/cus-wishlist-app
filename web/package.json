{
  "name": "web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "cd frontend && npm install && npm run build",
    "start": "npx prisma generate && node index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "prisma": "^5.7.0",
    "serve-static": "^1.15.0"
  }
}